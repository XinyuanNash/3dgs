# FastAPI service dependencies (runs on HOST, not inside container)
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
aiosqlite==0.20.0
pydantic==2.9.2
python-dotenv==1.0.1

# dev / testing (optional, install only if needed)
# httpx==0.27.2
# pytest==8.3.3
# pytest-asyncio==0.24.0